---
description: Technical architecture standards for Next.js, Strapi, Vercel, and SEO-optimized migration delivery
alwaysApply: true
---

# TECHNICAL ARCHITECTURE

## Purpose

Define implementation guardrails for rebuilding Desire Escorts on Next.js with Strapi as CMS and Vercel as deployment platform, with content optimization during migration to recover organic performance.

## Architecture Goals

- **Optimize-during-build** launch with content quality improvement over legacy.
- Strong SEO stability across routing, URL structure, and indexability.
- Content optimization per documented specifications.
- Clear separation between presentation (Next.js) and content source (Strapi).
- Fast, observable, and reversible deployments on Vercel.

## System Boundaries

- Frontend application: Next.js App Router.
- Content and entity source: Strapi (escort profiles, availability, page/blog content where modeled).
- Migration sources: WordPress exports + Firecrawl + sitemap-derived inventories + Search Console data.
- Hosting/runtime: Vercel preview and production environments.

## Routing and Locale Strategy

- Preserve existing language routing behavior:
  - NL default under `/`
  - EN under `/en/`
- Keep URL structure stable — no slug changes without redirect mapping.
- If route changes are unavoidable, define permanent redirects before release.
- Locale behavior must remain deterministic and aligned with canonical/hreflang output.

## Rendering Strategy

- Prefer static generation or cached server rendering for stable content pages.
- Use revalidation where content updates are periodic and not request-critical.
- Use dynamic rendering only where freshness is required (for example profile availability status).
- Avoid introducing client-side-only rendering for SEO-critical page bodies.

## Content Delivery Layer

- Encapsulate Strapi access behind a dedicated data layer (`lib/api` or equivalent), not directly in UI components.
- Normalize Strapi responses into frontend-friendly shapes before rendering.
- Enforce explicit error states and fallback behavior for missing/unpublished content.
- Keep content model mapping documented for:
  - pages
  - blogs
  - escort profiles
  - availability fields

## SEO and Metadata Contract

### Required Elements (Every Indexable Page)

- Canonical URL (self-referencing)
- Optimized title (50-60 chars, primary keyword front-loaded)
- Optimized description (150-160 chars, includes CTA intent)
- Robots/indexability intent
- Locale alternates (NL/EN hreflang mapping)
- Schema markup (page-type appropriate)

### Metadata Optimization Strategy

Metadata is optimized per Keyword Strategy (`KEYWORD_STRATEGY.mdc`):

| Element | Optimization Approach |
|---------|----------------------|
| Title | CTR-optimized, primary keyword front-loaded |
| Description | Action-oriented, includes differentiator and CTA |
| H1 | Contains primary keyword naturally |

### Heading and Content Structure

- Follow Content Specifications (`CONTENT_SPECIFICATIONS.mdc`) for page structure.
- H1 → H2 → H3 hierarchy with logical flow.
- Content structured for AI extraction (direct answers, lists, tables, FAQs).

### Schema Markup Requirements

| Page Type | Required Schema |
|-----------|-----------------|
| Homepage | Organization, LocalBusiness |
| Location pages | LocalBusiness, FAQPage |
| Attribute pages | FAQPage |
| Service pages | Service, FAQPage |
| Profile pages | Person, Review (if applicable) |
| Blog posts | Article, FAQPage (if FAQ present), Person (author) |

### Technical SEO

- Keep metadata generation centralized and deterministic across templates.
- Prevent preview/staging indexation (environment-specific noindex handling where required).
- Validate hreflang reciprocity (NL ↔ EN).
- Ensure canonical URLs are self-referencing.

## Internal Linking Strategy

- Preserve high-value internal linking pathways from legacy.
- Maintain hub-and-spoke structure from homepage.
- Cross-link related content (locations ↔ attributes ↔ profiles).
- No broken internal links.

## Media and Asset Strategy

- Preserve media references from migration inventories and avoid broken URLs.
- Optimize delivery through Next.js image pipeline where compatible with source constraints.
- Use explicit fallback behavior for missing images during migration rollout.

## Vercel Deployment Model

- Use branch/PR previews for validation before production promotion.
- Keep production config immutable through environment variables and project settings.
- Required environment variables must be documented and environment-scoped (preview vs production).
- Production releases must pass architecture QA gates:
  - URL structure checks (no unintended changes)
  - Content specification compliance
  - Metadata optimization checks
  - Internal link checks
  - Schema markup validation
  - Redirect validation

## Reliability and Observability

- Capture runtime errors for server and route handlers with actionable context.
- Log content fetch failures with URL/locale identifiers for fast diagnosis.
- Monitor launch health signals:
  - 404/500 trends
  - Crawler-visible regressions
  - Key template rendering failures
- Monitor SEO performance signals:
  - Position changes for priority keywords
  - CTR changes for optimized pages
  - Indexing status in Search Console

## Security and Compliance Baseline

- Never expose private tokens to client bundles.
- Use server-only access for privileged Strapi operations.
- Validate and sanitize any rich content rendered from CMS sources.

## Performance Baseline

- Optimize for Core Web Vitals on both desktop and mobile.
- Keep third-party scripts minimal and defer non-critical scripts.
- Maintain lean page payloads for key SEO landing pages.
- Target metrics:
  - LCP < 2.5s
  - INP < 200ms
  - CLS < 0.1

## Delivery Governance

- Content optimization must follow documented specifications:
  - `CONTENT_SPECIFICATIONS.mdc`
  - `KEYWORD_STRATEGY.mdc`
  - `SEO_OPTIMIZATION_GUIDELINES.mdc`
- URL structure changes require explicit approval and redirect mapping.
- Prefer additive, low-risk technical changes during migration.
- Record deliberate deviations and exceptions in migration artifacts.
