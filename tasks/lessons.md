# Lessons Learned

- Preserve legacy URL structure in both production and local development. Do not introduce convenience aliases (for example `/locaties`) when legacy parity requires existing slugs (for example `/escort-in-nederland`).
